<?xml version="1.0" encoding="UTF-8"?>
<grid-library>
	<grid-library-name>oracle-database11gR2-enabler</grid-library-name>
	<grid-library-version>${project.version}</grid-library-version>

	<!-- No JRE arguments -->

	<dependency>
		<grid-library-name>oracle-database11gR2-distribution</grid-library-name>
		<grid-library-version>${distribution.version}</grid-library-version>
	</dependency>

	<!-- No conflicts -->

	<jar-path>
		<pathelement>jars</pathelement>
	</jar-path>

	<!-- No library paths -->

	<!-- No assembly paths -->

	<!-- No command paths -->

	<!-- No hooks paths -->

	<!-- No environment variables -->

	<!-- No system properties -->

</grid-library>
